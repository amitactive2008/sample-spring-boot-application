apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-pvc
  namespace: issue-app
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: standard   # kind built-in StorageClass (local-path provisioner)
  resources:
    requests:
      storage: 1Gi
