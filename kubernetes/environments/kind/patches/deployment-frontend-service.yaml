# Patch: frontend-service — imagePullPolicy: Never (locally loaded image)
apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-service
spec:
  template:
    spec:
      containers:
        - name: frontend-service
          imagePullPolicy: Never
