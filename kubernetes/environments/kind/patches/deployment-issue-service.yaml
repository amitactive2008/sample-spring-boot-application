# Patch: issue-service — imagePullPolicy: Never (locally loaded image)
apiVersion: apps/v1
kind: Deployment
metadata:
  name: issue-service
spec:
  template:
    spec:
      containers:
        - name: issue-service
          imagePullPolicy: Never
